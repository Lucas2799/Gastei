<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

	<!-- Fundo (use a cor que quiser, pode ser @color/xxx) -->
	<item android:drawable="@color/white" />

	<!-- Se quiser gradiente em vez de cor, crie drawable/gradient_splash.xml e use aqui -->
	<!-- <item android:drawable="@drawable/gradient_splash" /> -->

	<!-- Imagem centralizada -->
	<item>
		<bitmap
            android:gravity="center"
            android:src="@drawable/splash_logo"
            android:antialias="true" />
	</item>

</layer-list>
