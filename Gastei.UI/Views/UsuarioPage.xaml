<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Gastei.UI.Views.UsuarioPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
     BackgroundColor="{AppThemeBinding Light={StaticResource PageBackgroundLight},
                                      Dark={StaticResource PageBackgroundDark}}"
    Title="Usuário">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="25">


            <!-- 🟣 HEADER PREMIUM -->
            <Frame Padding="25"
                   CornerRadius="20"
                   HasShadow="True"
                   Margin="0,10,0,10">

                <Frame.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#6a11cb" Offset="0.0" />
                        <GradientStop Color="#2575fc" Offset="1.0" />
                    </LinearGradientBrush>
                </Frame.Background>

                <VerticalStackLayout Spacing="10" HorizontalOptions="Center">
                    <!--<Image Source="user_settings.png"
                           HeightRequest="60"/>-->

                    <Label Text="Configurações do Usuário"
                           TextColor="White"
                           FontSize="26"
                           FontAttributes="Bold"/>

                    <Label Text="Gerencie suas preferências financeiras"
                           FontSize="14"
                           TextColor="#EEEEEE"
                           HorizontalTextAlignment="Center"/>
                </VerticalStackLayout>

            </Frame>



            <!-- 📝 FORMULÁRIO -->
            <Frame BackgroundColor="#1E1E1E"
                   CornerRadius="14"
                   Padding="20">

                <VerticalStackLayout Spacing="18">



                    <!-- Nome -->
                    <Label Text="Nome Completo *" TextColor="#AAA" FontSize="13"/>
                    <Border BackgroundColor="#292929" StrokeThickness="0"
                            StrokeShape="RoundRectangle 10" Padding="12">
                        <Entry x:Name="EntryNome"
                               TextColor="White"
                               Placeholder="Digite seu nome"
                               BackgroundColor="Transparent"/>
                    </Border>



                    <!-- Email -->
                    <Label Text="Email" TextColor="#AAA" FontSize="13"/>
                    <Border BackgroundColor="#292929" StrokeThickness="0"
                            StrokeShape="RoundRectangle 10" Padding="12">
                        <Entry x:Name="EntryEmail"
                               TextColor="White"
                               Placeholder="email@exemplo.com"
                               Keyboard="Email"
                               BackgroundColor="Transparent"/>
                    </Border>



                    <!-- Salário Base -->
                    <Label Text="Salário Base *" TextColor="#AAA" FontSize="13"/>
                    <Border BackgroundColor="#292929" StrokeThickness="0"
                            StrokeShape="RoundRectangle 10" Padding="12">
                        <Entry x:Name="EntrySalario"
                               TextColor="White"
                               Keyboard="Numeric"
                               Placeholder="R$ 0,00"
                               BackgroundColor="Transparent"/>
                    </Border>



                    <!-- Dia Recebimento -->
                    <Label Text="Dia do Recebimento do Salário *" 
                           TextColor="#AAA" FontSize="13"/>

                    <Picker x:Name="PickerDia"
                            BackgroundColor="#292929"
                            TitleColor="#AAA"
                            TextColor="White"
                            Title="Selecione o dia">
                        <Picker.Items>
                            <x:String>1</x:String>
                            <x:String>5</x:String>
                            <x:String>10</x:String>
                            <x:String>15</x:String>
                            <x:String>20</x:String>
                            <x:String>25</x:String>
                            <x:String>30</x:String>
                        </Picker.Items>
                    </Picker>



                    <!-- Perfil Financeiro -->
                    <Label Text="Perfil Financeiro *" TextColor="#AAA" FontSize="13"/>

                    <Picker x:Name="PickerPerfil"
                            BackgroundColor="#292929"
                            TitleColor="#AAA"
                            TextColor="White"
                            Title="Selecione seu perfil">
                        <Picker.Items>
                            <x:String>Equilibrado</x:String>
                            <x:String>Investidor</x:String>
                            <x:String>Conservador</x:String>
                        </Picker.Items>
                    </Picker>



                    <!-- Botão salvar -->
                    <Button Text="💾 Salvar Configurações"
                            Clicked="OnSalvarClicked"
                            HeightRequest="55"
                            FontAttributes="Bold"
                            CornerRadius="12"
                            BackgroundColor="#6a11cb"
                            TextColor="White"
                            Margin="0,20,0,0"/>

                </VerticalStackLayout>
            </Frame>




            <!-- ℹ️ INFORMAÇÕES DOS PERFIS -->
            <Frame BackgroundColor="#1E1E1E" CornerRadius="14" Padding="18">

                <VerticalStackLayout Spacing="10">

                    <Label Text="💡 Sobre os Perfis Financeiros"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="White"
                           HorizontalOptions="Center"/>

                    <BoxView HeightRequest="1" BackgroundColor="#333" Margin="0,5"/>

                    <Label Text="• Equilibrado — 50% Essenciais, 30% Lazer, 20% Investimentos"
                           TextColor="#CCC" FontSize="13"/>

                    <Label Text="• Investidor — 50% Essenciais, 40% Investimentos, 10% Lazer"
                           TextColor="#CCC" FontSize="13"/>

                    <Label Text="• Conservador — 60% Essenciais, 30% Lazer, 10% Investimentos"
                           TextColor="#CCC" FontSize="13"/>

                </VerticalStackLayout>

            </Frame>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
